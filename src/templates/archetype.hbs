/*global define*/
define(['jquery'], function ($) {

    'use strict';

    function {{module_name}}(config) {

        /* Store configuration. */
        this.CONFIG = {
            api_key: null,
            client_key: null,
            base_url: {{{base_url}}}
        };

        /* Extend default configuration. */
        this.CONFIG = $.extend(true, {}, this.CONFIG, config);

    }

    {{#each methods}}
        {{{this}}}
    {{/each}}

    return {{module_name}};

});