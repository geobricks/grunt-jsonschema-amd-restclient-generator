/*global define*/
define(['jquery'], function ($) {

    'use strict';

    function {{module_name}}(api_key, client_key) {

        /* Store configuration. */
        this.CONFIG = {
            api_key: api_key,
            client_key: client_key
        };

    }

    {{#each methods}}
        {{{this}}}
    {{/each}}

    {{module_name}}.prototype.isValidRequest = function() {
        return true;
    };

    return {{module_name}};

});